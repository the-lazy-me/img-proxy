# -------------------------------
# PixAI 图片代理服务配置
# -------------------------------

# 【核心配置】
# 自定义的图片访问域名（上传后返回的 URL 前缀）
CUSTOM_DOMAIN=http://localhost:8080

# 图片在 URL 中的路径前缀，例如 /img/xxx.jpg
PATH_PREFIX=img

# 允许访问的来源站点，用于 CORS 验证，多个用逗号分隔
ALLOWED_ORIGINS=https://your-frontend.com,https://*.your-domain.com,*

# API 密钥，用于保护 /proxy 接口，防止滥用
API_KEY=your-super-secret-api-key-here-please-change-it

# 文件过期时间（小时），超过此时间的文件将被自动删除
FILE_EXPIRY_HOURS=24


# 【存储配置】
# 本地存储的根目录（在 Docker 容器内）
STORAGE_PATH=/storage

# 服务器监听地址
LISTEN_ADDR=:8080